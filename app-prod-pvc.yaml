
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-pvc-prod
  namespace: finalapp-prod
spec:
  capacity:
   storage: 3Gi
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
